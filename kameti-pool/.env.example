# Optional. If omitted, the app uses http://localhost:4000 and appends `/api` (see src/lib/api.ts + backend/src/app.ts).
# You may set the full base including /api, e.g. http://localhost:4000/api
# VITE_API_URL=http://localhost:4000/api
