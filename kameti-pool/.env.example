# Backend API base URL (Express mounts the router at `/api` — see backend/src/app.ts).
# Use http://localhost:4000/api or just http://localhost:4000 (the client will append `/api`).
VITE_API_URL=http://localhost:4000/api
